services:
  segmentation-server:
    build: .
    image: house-segmentation-api:latest
    ports:
      - "5000:5000"
    environment:
      - MODEL_PATH=/app/saved_model.pth
      - PORT=5000
    restart: unless-stopped